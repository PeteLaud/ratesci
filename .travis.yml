# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

#language: R
language: r
r:
  - oldrel
  - release
  - devel
sudo: false
cache: packages
addons:
  apt:
    packages:
      - libmpfr-dev
